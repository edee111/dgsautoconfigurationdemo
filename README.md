### DGS Auto Configuration Demo

This project exists to show how autoconfiguration exlusion using `spring.autoconfigure.exclude` differs since DGS version `8.5.4` in combination with Spring Boot 3.3.
